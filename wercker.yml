box:
  id: openjdk
  tag: 8

build:
  steps:
    - xenoterracide/maven:
      goals: clean test

    - script:
      name: Create new git tag
      code: |
        echo -e "This is where we create the tag"

    - xenoterracide/maven:
      goals: jacoco:report coveralls:report deploy

    - xenoterracide/maven:
      goals: site

    - script:
        name: Upload reports
        code: |
          ./push_reports

